#include"Sort.h"
#include"pokersort.h"
//1、写一个排序函数接口，能完成对整数和小数的排序。Test1();

//2、接口中多传入一个缺省参数，如果不指定，就采用A排序方式，如果指定了，就按他的选择决定排序方式（至少包含三种）。Test2();

//3、在上述的基础上，完成对结构体的排序。通过传入判断大小的函数来实现。PokerTest();

int main()
{
	//Test1();
	//Test2();
	//Test3();
	PokerTest();
    system("pause");
    return 0;
}